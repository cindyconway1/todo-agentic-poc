 architecture; CSLA conventions (BusinessBase<T>, BusinessListBase<T,C>, register every property with RegisterProperty, validation in the rules engine via BusinessRules.AddRule, data access only in data portal [Fetch]/[Insert]/[Update]/[Delete] methods using the injected EF Core context, invoke via injected IDataPortal<T>); "every change needs a test" (rules → unit tests, persistence → integration tests); "migrations are the highest-review-priority artifact — call them out explicitly in the PR"; return typed DTOs with documented status codes so the OpenAPI spec is tight.